import ProductItem from '../components/ProductItem';
import { Container } from '../styles/contentStyle';

const Home = () => {
  return (
    <Container>
      <ProductItem />
    </Container>
  );
};

export default Home;
